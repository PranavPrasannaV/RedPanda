# ChessMamba v3 - dependencies
# Install: pip install -r requirements.txt

# Core ML
torch>=2.1.0
einops>=0.7.0
numpy>=1.24.0

# Chess
python-chess>=1.999

# Data conversion (Lichess eval dataset: parquet / jsonl.zst / csv)
pandas>=2.0.0
pyarrow>=14.0.0
zstandard>=0.22.0

# API server (engine/main.py) — optional
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0

# Progress bars
tqdm>=4.66.0
fastparquet>=2026.5.0

